# Pending private network comparison and observation (#161)

Status: **NotStarted** for live assessment and topology comparisons; **Blocked**
for a measured zero-egress conclusion until a minimized, attributable observer
method is ready and explicitly approved. This checklist grants no authority.
The separate frozen signed #160 candidate is unaffected. Source changes require
their own exact candidate identity and cannot inherit its signature or evidence.

1. Record the exact candidate source revision, generated artifact digest, runtime,
   approved private output destination, initiating user/session and chosen frozen
   network mode privately. Follow #160/#161's existing trust, protection and
   approval sequence. Decline one preparation and verify no assessment collection
   or package. Then approve one fresh Local Only run; do not widen its plan.
2. Compare the protected report against independently approved local read-only
   interface, IP Helper route, configured resolver, connected-profile and
   initiating-user Internet Settings/RAS observations taken in the same interval.
   Correlate numeric interface indices, address families, route metrics and
   multiple default routes. Include Unicode names and a disconnected interface
   when already available; do not reconfigure the device to manufacture a case.
3. Confirm incomplete, denied and bounded sources stay scoped while unrelated
   local evidence continues. Ethernet type must not imply physical hardware.
   Proxy coverage must disclose that WinHTTP, service-account and effective PAC
   decisions were not observed. RAS registration does not prove connection state,
   all-user inventory or third-party VPN coverage. Security Center currently has
   no approved offline live source: retain Unsupported and its stable reason.
   This missing native source is not a passed complete inventory; carry the gap
   to #161 and the relevant shared component owners.
4. Reopen the protected report offline. Verify local topology and interface
   relationships survive, network-dependent scopes are NotAttempted with reasons,
   and closing protected viewing proves owned plaintext absence. Keep exact
   network, identity, certificate and report values private.
5. Measure only after the observer method and required OS authority are approved.
   The observer must cover launch/preparation, decline, approved collection,
   nested workers, finalization and offline report viewing, with a finite time and
   storage bound, exact owned session cleanup, zero loss and complete request
   attribution. Validate observation sensitivity using an independently approved
   bounded loopback control; no external canary is authorized by this checklist.

Kernel socket metadata alone cannot attribute delegated DNS/service requests to
the assessment. The discovered DNS provider's broad informational selection
includes query results and other excessive metadata; its versioned ClientPID
does not by itself establish every delegated caller. No whole-provider activation,
packet/payload capture, audit/firewall changes, credential/command-line logging,
new tool, certificate/trust mutation or broad capture is authorized. No observer
was activated for #148. Resolve that finite attribution method gap before making
a measured zero-request claim; missing capture or missing attribution is Blocked,
not zero. The controlled adapter/source tests are separate implementation evidence.

The independently approved Microsoft Connectivity Enabled comparison belongs to
#150/#161, using its unchanged versioned target/operation catalog. Local loss or
blocked services must remain related-scope failures while local collection
continues. #158/#161 retain complete integration, resource/locale and delivered-app
acceptance. September 6 remains the private handoff target; no partial report or
fixture pass waives the full #37/#134 capability and quality requirements.
