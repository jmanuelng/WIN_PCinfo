# Issue 64 validation projection

This public projection contains release-owned contracts and identifier-free synthetic validation outcomes only. It contains no Assessment Record value, tenant identifier, device identifier, certificate fingerprint, proxy detail, package path, or protected evidence content.

## Observable evidence

| Gate | Public-safe result |
| --- | --- |
| Frozen rule policy | One offline release policy fixes five cross-domain rules, five ordered recommendations, three tenant-side discovery-task definitions, four typed recommendation relationships, four Microsoft source references, and eight closed validation scenarios. |
| Pure derivation | `CrossDomainGuidance.Tests.ps1` drives complete, partial, absent, denied, unsupported, stale, contradictory, and cross-domain synthetic records through the pure rule layer and verifies one outcome per cross-domain rule plus fixed recommendation, relationship, and task counts. |
| Generated application seam | `CrossDomainGuidanceApplication.Tests.ps1` runs the generated artifact through `DirectOutbound` and `LocalOnly` connectivity scenarios, then verifies one sanitized `win-pcinfo.cross-domain-guidance-validation` record, one terminal record, canonical record acceptance, beginner report verification, protected-package reopening, and cleanup. |
| Cautious ordering | The public projection proves the fixed `ImmediateReview`, `PlanNext`, and `ConsiderLater` tier counts and the fixed four-edge relationship graph without inventing a numeric score, hidden priority, or rollout date. |
| Privacy | The public projection is checked only at the cross-domain validation-record seam and proves that no tenant ID, device ID, thumbprint, proxy value, certificate fingerprint, synthetic connectivity payload marker, or TLS cipher string escaped into the issue-owned contract output. |

## Generated scenarios

- `DirectOutbound` proves that generic Microsoft connectivity alone is insufficient to complete the management-plane prerequisite: the rollup stays `Indeterminate`, the tenant-side enrollment-path task remains explicit, the ordered path remains fixed, and only bounded public counts leave the protected boundary.
- `LocalOnly` proves that zero outbound requests become `Indeterminate` guidance where required and that the additional tenant-side enrollment-path task appears instead of a fabricated cloud conclusion.

The fixture seam cannot supply a tenant, real certificate, proxy configuration, or production migration plan. The scenarios are synthetic and do not change the machine, the tenant, or any Microsoft service.
