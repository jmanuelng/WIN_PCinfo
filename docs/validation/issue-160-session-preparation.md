# Issue #160 first live assessment preparation

Scope: [#160](https://github.com/jmanuelng/WIN_PCinfo/issues/160), under
[#134](https://github.com/jmanuelng/WIN_PCinfo/issues/134) and
[#37](https://github.com/jmanuelng/WIN_PCinfo/issues/37). Start and fixed review
point: `1fc42761f3295ac7745d5c2ac10467326d9f5e13`.

Status: **Preparation complete; actual milestone Pending/Blocked on concrete
human-session gates**. No certificate creation, trust change, signing, live
assessment, metadata observation, release qualification or publication occurred.
This ticket cannot close on preparation evidence. September 6 remains the private
handoff target; complete delivered-workflow acceptance belongs to #161.

## Prepared private packet

The packet and instantiated ownership record are outside public source, with
explicit initiating-user/SYSTEM access and the environment's managed sandbox read
access recorded privately. They contain the exact private
paths, checked runtime/tool prerequisites, bounded prior-session reconciliation,
source snapshot, unsigned precursor, executable inventory, separate signing copy,
reserved output locations and pending observation worksheet. Existing identities,
keys and packages were not adopted or changed. Unexpected prior ownership or
residue blocks setup until reconciled; subject-first certificate selection is
prohibited.

The copied precursor matches the application and ZIP identities recorded in
[issue-137-status-desk.md](issue-137-status-desk.md). All **137 manifest resources**
passed exact digest/length checks and all **138 checksum rows** passed after the
copy. The executable inventory contains the generated application, PowerShell
launcher and CMD entry. CMD integrity remains governed by the signed application's
resource table. A frozen source snapshot prevents subsequent AFK work from
changing this candidate's later build inputs.

The packet makes the two candidate approvals distinct. Gate A covers the existing
exact unsigned launcher bytes plus the dedicated setup and narrow CurrentUser
trust operations. After signing the launcher, the existing build's SignedHelperPath
route embeds its signed digest into a new unsigned primary. That primary's actual
bytes require a separate approval before signing. No unknown future primary or
archive receives advance approval. The packet includes outer-manifest/checksum
refresh, exact signed archive extraction verification and disposable primary,
helper and governed-resource corruption rejection cases.

The approved separate signing/encryption design is retained through the procedure
in [personal-evaluation-certificates.md](../personal-evaluation-certificates.md).
The private worksheet covers the actual provider's synthetic recipient round
trip, independent local and recipient package opening, wrong/missing protector and
corruption rejection, view cleanup, retention and exact later removal. These
preparatory package checks do not substitute for delivered-candidate acceptance.

## Remaining actual gates

The orchestrator receives one consolidated packet of missing session authority
and unavoidable operator actions: exact setup/trust targets, known helper signing,
later concrete primary signing, recipient confirmation, finite live device scope,
any actual OS privilege/provider prompts, metadata-observer scope and owned
cleanup/retention. Unused acquisition, installation, Azure, external-service and
publication categories are explicitly NotApplicable for this session. No new
dependency is acquired and no execution policy is weakened.

The live checklist begins with portable double-click preparation and decline
without collection. A separate recipient-confirmed Comprehensive Local Assessment
in LocalOnly must reach real structured progress, an honest terminal and protected
HTML with an evidence-linked advisory finding, coverage limits and useful
follow-up. It records actual selected-source execution, UI and stage timings,
temporary plaintext absence and protected reopening. A private invocation record
can supply the existing launcher's recipient arguments without modifying portable
resource bytes or bypassing the GUI's approval control.

Measured egress is **Blocked pending observer authority and demonstrated
completeness**. Installed metadata-provider availability is only preparation
evidence. The packet excludes packet/payload capture and audit-policy mutation;
it requires bounded private observation, owned process attribution, loss checks
and honest treatment of delegated DNS/service activity. Missing attribution,
observer gaps or unavailable loss evidence cannot become a zero-request Pass.
Observer elevation does not grant product elevation. No additional observer scope
may be inferred from this packet.

No broad-collector blocker has been observed in this preparation. The conditional
interim profile is therefore neither selected nor implemented. Any actual failed
gate remains Fail/Blocked with its bounded repair owner. Existing keys, protected
recoverable evidence and ownership journals survive failure until retention-safe
disposal is proved. No collection resume or broad trust/cleanup expansion occurs.

## Validation and requirement contribution

Implement and TDD were applied for applicability: this slice changes only excluded
validation documentation and prepares private files. It introduces no production
behavior or executable helper. Red/green code tests and typechecking are
**NotApplicable**; no mirror tests were added. Focused verification consists of
the exact copied resource/checksum checks, source/input identity checks, private
path/ownership checks, command/API inspection and `git diff --check`. The unchanged
#137 combined 132-file final-input regression and its separate reviews are reused;
no full suite or unchanged production-code review was repeated. Actual live checks
are explicitly unperformed.

Code Review examines only this preparation delta from the fixed point, with
independent Standards and Spec axes. Their final results are recorded in the
completion handoff; no live acceptance follows from either review.

| Requirement contribution | Current evidence | Remaining owner |
| --- | --- | --- |
| #134 GUI 25–28,32: separate identities, real recipient, exact candidate and removal | Concrete private commands, staged helper, two signing gates, ownership/retention worksheet | #160 actual setup/signing/round trip; #161 complete recovery acceptance |
| #134 GUI 1,4–9,13,15,19–24,29: first delivered GUI-to-HTML milestone | Ordered decline/approve/real source-to-report observation checklist | #160 actual first milestone; #161 final breadth and controls |
| #37 stories 21,23: LocalOnly request boundary | Metadata observation plan with explicit attribution/loss limitations | #160 measured first run; #148/#161 full network evidence |
| CAP-0015,0018,0019,0020,0024,0025 and shared components | Preparation only; no capability closure | #158 aggregates requirement rows; owning implementation/live slices retain obligations |

Parents #134/#37 are unchanged. The orchestrator owns GitHub operations and the
requirement register. Public output contains only sanitized status and limitations;
the instantiated packet, fingerprints, paths, profiles, signatures, real records,
screenshots and measurement details remain private.
