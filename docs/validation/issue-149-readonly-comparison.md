# Pending private certificate comparison (#161)

Status: **NotStarted** for live certificate collection and source comparison.
Measured Local Only zero egress remains **Blocked** on the minimized delegated
DNS/service attribution method carried by #148/#160/#161. This checklist grants
no new collection, certificate, trust, signing, observer or setup authority.

1. In the approved private area, bind the comparison to the exact source revision,
   generated candidate digest, runtime, initiating Assessment User, selected
   network behavior and frozen preparation. A changed candidate needs its own
   signing/admission evidence. It cannot inherit the separate private #160
   candidate's signatures. Confirm that preparation and decline perform no
   assessment store reads; approve collection only through the existing workflow.
2. Compare only the frozen purpose/EKU pairs: Management and Authentication in
   CurrentUser/My; DeviceIdentity in LocalMachine/My; CodeTrust in CurrentUser and
   LocalMachine TrustedPublisher. Use an independently approved read-only source
   comparison in the same interval. Do not broaden into root, other-user or full
   unrestricted certificate inventories. Keep exact certificate identifiers,
   dates, fingerprints, store contents and user/device context private.
3. Compare each admitted candidate's purpose, store, validity interval, current
   validity, offline chain completeness, local trust and observed key-presence
   state against the protected record and report. Preserve initiating-user and
   machine-store provenance. Key presence does not establish exportability or
   access; never open/export a private key, inspect PFX/passwords, create a
   certificate, alter trust or add recipient access to manufacture a case.
4. Use pre-existing authorized cases for valid, expired and not-yet-valid leaves;
   untrusted complete and incomplete offline chains; multiple candidates; absent
   purpose; inaccessible stores; virtual devices; and alternate-admin context.
   Missing environmental cases remain NotStarted/Blocked with the next owner,
   not Passed. A missing EKU is omitted from purpose selection. A ninth matching
   candidate gives Constrained coverage; no claim is made about the unexamined
   remainder. A partially accessible CodeTrust scope retains observed candidates
   but cannot claim complete trust readiness. An alternate administrator supplies
   neither the initiating user's store evidence nor successful absence.
5. Verify each purpose's advisory validity/trust outcomes, exact internal evidence
   references and follow-up recommendations after protected package reopening.
   Verify that HTML retains store contexts, scoped reasons and the separation
   between assessment observations, signing trust and Package Recipient setup.
   TLS inspection and service purposes without attributable approved targets
   remain explicitly NotApplicable; neither root presence nor certificate
   differences prove inspection or remote service acceptance.
6. Evaluate Local Only request behavior only after the approved observer method
   covers actual worker requests and delegated Windows services with finite
   bounds and owned cleanup. NoCheck and disabled intermediate downloads are
   required before any chain build. Incomplete offline chains stay explicit;
   they do not authorize AIA, revocation, enrollment, DNS or other retrieval.
   Controlled adapter tests establish code behavior, not observed zero egress.
   Microsoft Connectivity Enabled target/TLS comparison belongs to #150/#161
   under its separate unchanged approved catalog.
7. Close protected viewing and verify owned plaintext absence. Retain protected
   packages, recipient keys and recovery ownership as required by the existing
   private workflow. Publish only sanitized result counts, timings, dispositions
   and next-owner references. Keep source comparisons, identities, reports and
   screenshots private. #158/#161 still own complete integration, resource/locale,
   GUI workflow and delivered-app acceptance; September 6 remains the target.
