# Local topology and Local Only implementation (#148)

Starting revision: `ec79d3799d76634abc184c104ea7ceba60ef1c59`. Final application/schema revision: `d41e0255f160d180ef662b2add07856c9d49ea86`. #137 is merged;
#142 was merged while this slice ran. This evidence establishes controlled-source
implementation behavior, not a live assessment, measured zero egress, application
acceptance, signing or release qualification. No observer, UAC, live collector,
network configuration change, certificate/trust mutation or signing was performed.

The existing native offline topology collector and Comprehensive engine remain
in use. Three declared Integer fields retain profile, route and resolver Windows
interface indices through the canonical record, protected package and HTML.
Contract set 1.13.0 declares this additive change; older package manifest versions
remain admitted. The finite field-definition ceiling increases by exactly three;
the topology transport, item, deadline and rule limits are unchanged.

Ethernet interface type no longer claims a physical hardware flag. Loopback and
tunnel types remain known non-hardware; other types retain SourceReportedUnknown.
Invalid proxy enable values/types and malformed or oversized proxy projections
now produce scoped Malformed coverage without an invented disabled observation.
The report explains current-user Internet Settings and RAS coverage, unobserved
WinHTTP/service/PAC and all-user/third-party contexts, and the existing unsupported
offline Security Center source. Registered component metadata is not health.

Offline certificate chain evaluation now requires the download-disable property
before Build, retaining NoCheck. A missing capability fails before evaluation.
Microsoft documents that [DisableCertificateDownloads controls AIA issuer
retrieval](https://learn.microsoft.com/en-us/dotnet/api/system.security.cryptography.x509certificates.x509chainpolicy.disablecertificatedownloads).
The change does not implement #149's broader certificate slice or #150 connectivity.
Both approved network choices and their existing catalog remain intact.

## Controlled validation

Installed PowerShell Core 7.6.5 X64 runs the existing harness. The new generated
source adapter replaces only OS reads, process-context observation and the
topology snapshot transport in test memory; it is not shipped as a CLI or live
source authority. It executes the actual local PowerShell reducer, item admission,
ordinary scheduler, typed record, rules, encryption, reopening, HTML and protected
viewing. Native C# types compile without invoking their Windows sources. This
matrix does not prove actual COM/IP Helper call behavior or native worker process
attribution. Replacements have exact unique anchors and stop if source boundaries
change. Unrelated capability prerequisites use existing controlled adapters.

The request checks observe actual adapter dispatch, not the collector's declared
outboundRequestCount. Before approval and after decline, topology/request adapters
remain unentered. The actual LocalOnly connectivity branch cannot enter the
request-capable probe adapter. A separate controlled certificate API executes the
release's exact chain configuration/Build statement, checking NoCheck and disabled
downloads at the call boundary, including the missing-capability negative. Report
checks reject remote assets; native topology source guards forbid request-capable
APIs and provider activation. These controlled checks cannot measure delegated
Windows service traffic and are not a live zero-egress conclusion.

Observed red cases: missing typed route interface identity; virtual Ethernet
claimed as hardware; malformed proxy became a complete disabled result; oversized
proxy became a partial disabled result; missing certificate offline capability
still reached Build; report omitted proxy/VPN source-context limits. Retained
regressions now pass. Test-development-only failures included an outdated field
ceiling, scope aliases superseded by connectivity, and empty-array assertions;
these were corrected and are not live evidence.

| Focused gate | Result |
| --- | --- |
| NetworkSourceApplication | Pass: Complete, Disconnected, Denied, Partial, MalformedInterface, MalformedProxy, ProxyDenied; 21.1–22.6 s per case. Final oversized-proxy case passed in 22.3 s. Each uses actual generated protected record/report and owned cleanup |
| StatusDeskEngine Complete and DeclinePreparation | Pass: post-change context guidance, approved source execution, no preapproval/declined source or request-adapter entry, no declined package |
| NetworkTopologyContract | Pass: canonical source evidence, related interface indices, scope coverage and bounded findings |
| NetworkTopologyApplication | Pass: all 14 existing synthetic topology fixtures, approved-enabled fixture and invalid fixture; package reopening, HTML, privacy and cleanup |
| NetworkTopologyPolicy / NetworkTopology / NetworkTopologyNativeSource | Pass: frozen policy, scoped malformed/denied/partial evidence, 160,196-byte admitted worst-case payload, exact 262,144-byte transport acceptance and one-byte-over rejection |
| LocalOnlyRequestBoundary / CertificateTrustNativeSource / MicrosoftConnectivityPolicy | Pass: controlled chain/request attempts, offline source safety and unchanged enabled catalog |
| AssessmentContractSet | Pass: version 1.13.0, 262 field definitions, 104 distinct scopes and existing definition admission |
| SchemaContracts | Pass (12.0 s before extra negatives; final negative set also passed): both modes and closed plan admission, including dropped topology identity fields, widened offline mode/bounds and omitted discovery tasks. Initial device/identity constant drift was corrected |
| ProtectedPackageContracts / DeviceReadinessPolicy / CertificateTrustPolicy | Pass: manifest/envelope schema and offline collector/source authority (1.5 / 0.6 / 0.6 s) |
| PolicyUserContextNativeSource | Pass: native source compilation without calls, exact nested SYSTEM source preservation and eight configured launch samples; maximum 31,985 of unchanged 32,500 characters (2.6 s) |
| BuildDeterminism | Pass: independent directories, exact resource provenance and standalone build checks (51.5 s) |
| Changed executable PowerShell syntax / diff whitespace | Pass |

The schema consistency corrections are confined to the already changed
preparation schema: sourceBounds remains activationRows=16, enclosureRows=1,
chassisCodes=8, batteryRows=1, overflowProbeRows=1; identity discoveryTasks now
includes the existing approved-administrator and recovery-escrow tasks from #141.
Both align exact existing release constants, preserving closed admission and
unchanged source/behavior/bounds. A temporary edit hit the wrong discovery array;
the schema gate rejected it, the edit was corrected to identityEnrollment, and
the unrelated firmware constant has identical parsed content to the committed
original. The final schema gate and added negative mutations passed.

Exact unsigned candidate from final application/schema revision `d41e025`:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| WIN-PCInfo.ps1 | 3,159,845 | `3fbb460b93b7df1e54299f8691ea2ed275ab18505101f46511394de45f1d754f` |
| WIN-PCInfo-2.0.0-preview.1-portable.zip | 4,679,973 | `c4d63fff444968f0346ccd7420b52a8d008400f61664ea5592ba8ea3047a06d6` |

Both match both deterministic-build copies. Content-tree identity is
`881d4d03172cf13282dd1793a773191cccf711a5aff5cde41ae4715415e882b7`.
Ignored synthetic build outputs remain local; generated protected test evidence
and viewing are cleaned by existing owned cleanup. The separate signed private
#160 candidate is unchanged. Evidence-only edits do not renew signing/live gates.

## Standards review

Independent review of `ec79d379...5eb43e8`: zero documented-standard violations.
One nonblocking judgment notes six pre-existing branches returning the same
contract-version literal in DeviceReadiness. A broader version-selection refactor
is unnecessary for this slice. Explicit field/transport bounds, honest
unknown/malformed evidence, offline chain control, controlled-source authority
and DCO were accepted. The affected `5eb43e8..d41e025` schema/test/evidence review found zero violations or new judgments and independently verified all candidate/copy digests and exact existing constants.

## Spec review

Independent review of `ec79d379...5eb43e8`: zero blocking implementation findings
or scope creep. The reviewer confirmed typed identity/provenance, protected
record/report, LocalOnly adapter boundaries and honest live/source gaps. Final
focused checks and exact candidate evidence are now recorded above. The
orchestrator must publish the requirement-owner contribution into the shared
#134 register contribution through the merged document and durable orchestration ledger during delivery; #158 owns aggregation, and parent issues remain unchanged. The affected `5eb43e8..d41e025` schema/test/evidence review found zero findings and independently confirmed the exact six-task constant, closed admission and honest pending live gates.

## Requirement register contribution

| Requirement | Implementation status | Next owner |
| --- | --- | --- |
| #37 stories 21,23,44; CAP-0011; CMP-0011/CMP-0022 | Pass at controlled generated source/report seam: typed topology/context, scoped gaps and offline request boundary | #161 real source comparison and attributable observation; shared connectivity contribution #150 |
| #37 stories 24–25,50–54,66–67,69; #134 GUI stories 8,19–21,24 | Pass for bounded #148 contribution: unrelated local continuation, consent, protected evidence/HTML and explicit network NotAttempted states | #150 connectivity, #151 report, #158 integrated regression, #161 delivered-app acceptance |
| Native Security Center inventory and broader proxy/VPN contexts | Unsupported/unobserved in the approved offline source; no complete-inventory or health claim | #161 retains actual gap and shared component follow-up; no fixture closes the live gap |
| Measured no-request behavior before approval, LocalOnly, nested DNS/certificate/report paths | Controlled adapter/source guards pass; live measurement Blocked by unresolved minimized delegated-request attribution | #160/#161 approve and validate the concrete observer method |
| #37 sub-objectives 7–9, full #134 scope, resource/locale/release gates | Still pending comprehensive integrated and actual-device acceptance; CMP-0061 remains deferred | #158/#161 and remaining release owners |

The [private comparison and observation checklist](issue-148-readonly-comparison.md)
preserves all pending live actions and the method gap. Whole-repository regression
is reserved for #158/final under the user's focused-test cadence. Historical
#138 working-set measurements of 662–755 MiB exceed the provisional 512 MiB
budget; no shorter test or source-size result waives it. September 6 remains the
complete private handoff target. Parent #37/#134 are unchanged.
