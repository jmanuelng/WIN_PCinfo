# Private identity/source comparison for #161

This procedure is unexecuted. #161 owns authorization, the exact personally
signed candidate and the approved private destination outside the repository.
It does not authorize elevation, collection, account changes, directory access,
tenant authentication, certificate operations or signing. Keep instantiated
records, account/session/SID facts, protected packages, HTML and screenshots
private. Publish only sanitized dispositions and counts.

1. Record the candidate digest, runtime, profile, network behavior, protection
   scope and permitted context. Complete ordinary preparation and one approval.
   Compare near collection time; session and account state can change.
2. Inspect workgroup, domain-joined, Entra-joined and registered examples only
   where such environments are available and authorized. Do not join, unjoin,
   register or enroll a device merely to create a test case.
3. Compare the restricted record with the narrow read-only sources below.
   Preserve native return codes and context privately. Do not export whole
   native structures, certificates, registry trees, account lists, credential
   data or unrestricted command output. Do not resolve names against a domain.
4. Trace each observation through provenance and coverage to its exact rule
   evidence references, consultant follow-up, encrypted package and offline
   HTML. Check Completion Summary coverage and package availability agree.

| Comparison | Expected evidence and limitations |
| --- | --- |
| `NetGetJoinInformation(NULL)` | Typed domain/workgroup state and domain name; failure/denial is not workgroup absence. |
| `NetGetAadJoinInformation(NULL)` in the verified user's context | Default device join or one work account only. No join may return null; unavailable is distinct from no join. Device-default Entra join does not establish absence of user work accounts. Retain only the approved join/device/tenant fields privately. |
| WTS active-session enumeration and local LSA logon-session SID binding | One fully inspected active interactive session, SID and session ID. Compare querying token and session privately. Ambiguous, denied or missing context cannot be replaced with initiating user, alternate administrator or SYSTEM. |
| Same user, different process user, different session, elevated coordinator, alternate administrator and SYSTEM | Approved cases must show actual context and honest denied/unavailable user evidence. A later session change must not admit the former user's account. Do not induce an interactive switch during active assessment without explicit scenario authority. |
| Built-in Administrators SID `S-1-5-32-544`, local `NetLocalGroupGetMembers` level 0 | Direct SIDs only; unresolved names remain unknown. Eight-member bound gives Partial when exceeded. Denial/failure must not look like an empty group. No nested or remote name resolution. |
| Predefined SYSTEM operation: `Root\cimv2\mdm\dmmap`, `MDM_DeviceManageability_Provider01_01` presence | Administrator token alone is insufficient. Successful absent provider differs from Denied or Unavailable. Provider presence is not enrollment, assignment or compliance. No broad class dump or new query. |
| Tenant discovery and recommendation links | Authorized organization roles receive questions for assignment, licensing, compliance, enrollment intent, approved administrator context and escrow. No local fact proves these. Escrow verification must not retrieve, display or store recovery secrets. |

For every case record expected/observed scope, exact source return disposition,
value state, rule outcome, follow-up, elapsed time and Pass/Fail/Blocked or
justified NotApplicable. An unexecuted selected source is not a validated
capability. Missing environments remain pending; fixtures do not supply live
evidence. Collect Local Only service-mediated attribution through #148/#161's
approved observer procedure, not through an ad hoc capture.

Reopen the final encrypted package with the initiating-user and approved
recipient routes. Verify an alternate administrator receives no implicit local
protector. Close viewing and prove owned temporary plaintext, workers, pipes
and transient SYSTEM task absent. Preserve recoverable protected evidence and
ownership records if cleanup is uncertain. No automatic plaintext export or
forensic-erasure claim is permitted.

Source-specific user RSOP verification belongs to #142. This procedure does not
accept process SID as verified Assessment User policy provenance. #158/#161
must include that correction in the eventual complete candidate, together with
the existing unresolved resource-budget and remaining live acceptance gates.
