# Issue 53 validation projection

This public projection contains release-owned contracts and identifier-free synthetic validation outcomes only. It contains no GPO identity, link, setting identity, policy value, local account-policy value, principal SID, package path, or private Assessment Record value.

## Observable evidence

| Gate | Public-safe result |
| --- | --- |
| Frozen authority | One offline Administrator operation freezes five structured sources, fifteen Evidence Scopes, finite catalogs, exact bounds, no installation, no refresh, and no write authority in the immutable Preparation Plan. |
| Three layers | Applied Policy Evidence, Configured Policy Signals, and Current Control State retain independent coverage and cannot substitute for one another. |
| Locale-neutral sources | Cached RSoP CIM classes with one finite supported setting extension, NetAPI, Audit Policy, LSA, and exact read-only registry APIs are used; localized report and command-output parsing are prohibited. Registry freshness is explicitly unobservable through the approved live source. |
| Evidence honesty | The protected request binds cached user RSoP to the initiating Assessment User SID. Empty RSoP is observed absence; independent class/NetAPI failures, missing, stale, denied, malformed, unsupported, and partial sources remain explicit gaps and yield Indeterminate findings. A ninth policy object, setting, or directly assigned principal is never silently truncated: only the bounded subset is retained under explicit Partial coverage. |
| Interpretation | Three finite rules report applied coverage, local-security coverage, and observed order conflict without compliance, organization-intent, or remediation claims. |
| Privacy | Generated public output is checked for synthetic policy GUIDs, links, registry names, and principal SIDs; only states, counts, findings, and fixed safety assertions are projected. |
| Generated application | Fourteen closed cases exercise Preparation, the bounded Administrator worker, SYSTEM-denial context, canonical record, beginner report, package reopen, one terminal outcome, and verified cleanup. |

The fixture seam cannot supply a policy object, link, setting, value, SID, command, script, executable, source path, network permission, or mutation. The scenarios are synthetic and perform no policy refresh, import, export, edit, installation, or enforcement action.
