# Issue 54 validation projection

This public projection contains release-owned contracts, identifier-free synthetic outcomes, and public-safe assertions only. It contains no tenant identifiers, assignment state, Policy CSP values, MDM object identifiers, precedence details, package path, or private Assessment Record value.

## Observable evidence

| Gate | Public-safe result |
| --- | --- |
| Frozen SYSTEM scope | One predefined offline SYSTEM operation freezes its registry build source, provider class, two exact Policy Result nodes, five Evidence Scopes, signed host, dependencies, attempt/deadline/output bounds, and cleanup proof. The channel names the fixed restricted-evidence contract honestly. |
| Build and value gating | Unsupported or future build, missing class/property, empty or multiple node, query failure, duplicate field, malformed Boolean, oversized integer, and out-of-domain option cases remain distinct `Unsupported`, `Unavailable`, or protocol-integrity outcomes. Missing or malformed data is never reinterpreted as `false`, `0`, or not configured. |
| Scope limit | `MDMWinsOverGP` is observed only for the documented Policy CSP conflict scope. Generated guidance does not generalize it to all MDM, Intune, CSP, or tenant policy. |
| Conflict signal | Three bounded local security-option signals can be compared against the matching Policy CSP result fields. A disagreement becomes `NeedsAttention`, not a guessed winning source, compliance verdict, or remediation instruction. |
| Discovery follow-up | Incomplete Policy CSP coverage or an observed cross-channel disagreement produces tenant-side discovery tasks instead of an assignment or precedence guess. |
| Privacy | Progress and public validation output keep Policy CSP values, MDM object identifiers, and precedence details out of the public projection. Only states, findings, counts, and fixed safety assertions leave the protected package. |
| Provenance | Restricted Policy CSP observations retain the SYSTEM collector, operation, execution context, and collection time; the Administrator policy collector owns only its local sources. |
| Generated application | Twenty closed effective-policy fixtures cover non-MDM, unsupported build, missing class/property, scoped `MDMWinsOverGP`, conflicting channels, packaging, cleanup, and identifier-free public output. |

The synthetic seam does not authenticate to Microsoft services, evaluate Intune assignment, infer compliance, sync state, or organization intent. It performs no enrollment, policy refresh, CSP write, tool installation, or network activity.

## Final validation

- Standards and specification reviews completed with no remaining actionable findings.
- The 20-case SYSTEM matrix and 20-case generated Effective Policy application both passed, including contradictory-state, exact-attempt binding, cardinality, query-failure, and integer-bound regressions.
- `pwsh -NoLogo -NoProfile -File ./tests/Run-Tests.ps1` passed all 89 test files in 2,046.97 seconds.
- The final deterministic generated-application SHA-256 is `bacf44ea7bbe3316e15e71c905b503b307b00837b0c331ca0f37dce16aa393f3`.
